# =============================================================================
# STC Category Configuration
# =============================================================================

category: stc
pattern: "stc_"

default_stages:
  - Environment Setup
  - Copy Scripts
  - Clone Repositories
  - "Repository Init (sub-modules & deps)"
  - Codebase Configuration
  - Build
  - "Test-Only: Device Verification"
  - Device Preparation
  - Test Execution
  - Report Generation
  - "Declarative: Post Actions"

default_stage_poc: "pavan7.vs@partner.saung.com"

stage_poc_overrides: {}

jobs: {}
  # Add STC jobs here
